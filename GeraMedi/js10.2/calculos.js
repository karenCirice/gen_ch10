const suma = (a, b) => a + b;

const resta = (a, b) => a - b;

const multiplica = (a,b) => a * b;

const divide = (a,b) => a / b;

module.exports = {suma, resta, multiplica, divide}; /* Para poder llamarlo */

/* El script en package se cambió para que nos muestre diferente en consola/gitBash */
/* ESTE ES EL ARCHIVO ORIGINAL, EL MODULE EXPORTS NOS SIRVE PARA PODER LLAMAR DESDE EL ARCHIVO DE TESTEO */