let numeros = [1, 2, 3, 4, 5];
let doble = [];

        for (let i = 0; i < numeros.length; i++){
 doble[i] = (numeros[i] * 2); 
     //      doble.push(numeros[i] * 2);  Esta es otra manera
}
console.log (doble);
